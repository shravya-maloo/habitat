# Postgres connection string. For a free hosted database, create a project at
# https://neon.tech and copy its connection string here (use the "pooled"
# connection string if Neon offers one — it's the one meant for serverless).
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
